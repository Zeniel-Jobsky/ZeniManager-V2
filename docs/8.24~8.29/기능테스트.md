# 기능 테스트 체크리스트 — 2026-08-25 기준

> 작성자: BE-wookhyun (Claude Code와 함께 진행)
> 관련 문서: [0825-results.md](./0825-results.md)

8/25에 스키마를 `clients`/`sessions` 실DB에 맞추는 대규모 작업을 하면서 코드가 안 건드린 화면까지 포함해 곳곳이 영향을 받았다. 이미 확인된 것도 있지만, **아직 실사용으로 확인 안 된 것 위주로** 정리한다. 우선순위는 위험도(옛 스키마 흔적이 남아있을 가능성) 순.

체크박스는 실제로 눌러보고 정상 동작하면 `[x]`로 표시.

---

## 🔴 확실히 깨져있는 것 (테스트 아니라 수정부터 필요)

- [ ] **관리자 고객 목록 → 엑셀/CSV 가져오기(임포트)**: `AdminClientList.tsx`의 가져오기 기능이 여전히 옛 `client`(단수) 스키마 필드명(`gender_code`, `business_type_code`, `capa`, `desired_job_1`, `memo`, `address_1`, `school_name`, `iap_to` 등)으로 payload를 만든다. `createClient`가 이 필드들을 모르기 때문에 **이름/전화 등 일부만 저장되고 나머지는 조용히 누락**될 것으로 보임. 아직 코드 수정 안 함.
- [ ] **취업 성공사례 AI 매칭** (`ClientSummaryAnalysisTab`에서 트리거되는 `employmentSuccessCase.ts` + Edge Function 3개): 여전히 옛 `client`(단수, 정수 PK) 스키마를 참조 중. 클릭하면 404/오류 예상.

---

## 🟡 코드는 고쳤지만 미검증

### 로그인 / 계정
- [x] 상담사 여러 명 각자 로그인 → 본인 이름/역할 정상 표시
- [x] 로그아웃 → 재로그인

### 고객(상담자) 목록 — 상담사용 (`/clients` `ClientList.tsx`)
- [x] 담당 고객만 표시됨 (확인됨)
- [x] 검색 (이름/전화번호 등)
- [x] 정렬 (컬럼 헤더 클릭)
- [x] 컬럼 순서 드래그로 바꾸기 / 너비 리사이즈 — 바꾼 뒤 새로고침해도 유지되는지
- [ ] 점수/사후관리(`retention_1m_yn`)/메모 컬럼 값 표시
- [x] 왼쪽 스와이프 → 삭제 (확인됨)
- [x] 페이지네이션 (고객 20명 넘을 때)

### 고객 등록 (`ClientRegister.tsx`)
- [x] 기본 등록 성공 (확인됨)
- [x] 필수값 누락 시 검증 메시지
- [x] 주민번호 앞자리로 생년월일/나이 자동계산 로직 --> 00년생 이후 수정필요
- [ ] 취업완료 상태로 등록 시 취업처 필수 검증 + `employmentSuccessCase` 동기화 호출이 에러 없이(또는 예상된 에러로) 처리되는지 --> 노동청확인로직이 필요 ? 단순 입력으로 가능한지 ? 

### 고객 상세 (`ClientDetail.tsx`)
- [x] 인라인 필드 수정 (확인됨) — 아래는 필드별 세부 확인 권장
  - [x] 나이, 주소, 최종학력, 대학교명, 전공
  - [x] 역량등급, 참여단계, 참여유형, 배정구분, 사업유형
  - [x] IAP수립일, 초기상담일(읽기전용), 재진단일/여부(="보유점수" 라벨로 표시되는 필드)
  - [x] 희망직종
  - [x] 취업처/취업직무/취업일자/급여
  - [ ] 고용유지현황 1/6/12/18개월 (날짜+유지여부)
  - [x] 상담 가이드 및 특이사항(`counsel_notes`) 별도 편집 버튼
- [x] 자격증/이메일/MBTI/자차·알바·운전여부/희망지역/희망급여 등 **삭제된 필드 UI가 화면에서 잘 빠져 보이는지** (레이아웃 깨짐 없는지)
- [ ] 상담 이력 탭
  - [ ] 세션 목록 조회
  - [ ] 세션 상세 보기/수정 (상담 내용, 다음 조치사항)
  - [ ] 신규 상담 기록 등록 (유형/일자/내용/다음조치)
  - [ ] 세션 삭제
  - [ ] 세션 등록 시 고객 참여단계 자동 업데이트되는지
- [ ] 참여수당 섹션 — 스텁 처리돼 있어 등록 시도하면 "지원되지 않는 기능" 에러 토스트가 뜨는지(깨지지 않고 에러 메시지가 명확한지)
- [ ] 구직준비도 설문 탭 (`job_search_survey` 테이블) — ⚠️ 이번 스키마 작업 때 손 안 댄 영역이라 실제 컬럼명이 코드(`survey_1`~`survey_8`, `survey_7_memo`)와 맞는지 자체가 불확실함. 열어서 정상 표시/저장되는지 우선 확인 필요
- [ ] 요약분석 탭 (`ClientSummaryAnalysisTab`)
- [ ] AI 채팅 탭 (`CounselChatTab`) — 프로필 요약에 최근 줄인 필드(학교/전공/사업유형/희망직종/주소/메모)만 정상 반영되는지

### 관리자 — 고객 목록 (`AdminClientList.tsx`)
- [ ] 전체 고객 목록 로딩 (필터: 지점/단계)
- [ ] 이름 정렬 (수정함 — `client_name`→`name`)
- [ ] 역량등급/점수/IAP수립일 컬럼 (신규 추가)
- [ ] CSV 내보내기 — 실제로 열어서 값이 제대로 채워지는지 (필드 매핑을 새 스키마로 재작성했음)
- [ ] (가져오기는 위 🔴 항목 참고 — 지금은 테스트해도 정상 동작 안 함)

### 관리자 대시보드 (`AdminDashboard.tsx`)
- [ ] KPI 카드 (총원/진행중/취업완료/성사율)
- [ ] 프로세스 단계별 인원 차트
- [ ] 월별 성사율 추이
- [ ] 사업유형별 파이차트
- [ ] 지점/상담사별 테이블

### 관리자 — 상담사 목록 (`CounselorList.tsx`)
- [ ] 상담사별 담당 고객 수 / 취업완료 수
- [ ] 관리자 메모(`manager_memo`, 이번에 새로 만든 테이블) 작성/조회 — 반드시 확인 필요 (신규 테이블이라 실사용 이력 없음)

### 상담사 대시보드 (`Dashboard.tsx`)
- [ ] 통계 카드 (총 고객/진행중/취업완료/후속관리 필요/평균 점수/점수 분포)
- [ ] 월별 추이 그래프
- [ ] 캘린더 (일자별 상담 건수, 상세 목록)
- [ ] 개인 메모장 (`fetchMyMemo`/`updateMyMemo`, `public.user.memo`) 저장/조회
- [ ] 고객 검색 (`searchDashboardClients`)
- [x] 좌측 상단 로고 클릭 → 대시보드 이동 (확인 필요 — 역할별 분기 로직 추가함)

### 기타
- [ ] 설정(Settings) 화면 — Supabase URL/키 변경 후 정상 재연결되는지
- [ ] 관리자 ↔ 상담사 역할별 라우트 접근 제한이 이번 작업으로 깨진 곳 없는지

---

## 참고: 이번에 스키마와 무관해 안 건드린 영역

- `job_search_survey`(구직준비도 설문) — 위에 표시한 대로 실제 컬럼명 불확실, 우선 확인 대상
- `memo_cards`(칸반 메모 카드) — 코드(`fetchMemoCards` 등)는 있지만 **실제로 호출하는 화면이 없음**(미사용 확인). 대시보드의 "메모장" 탭은 이것과 무관하게 `public.user.memo` 기반 개인 메모임
